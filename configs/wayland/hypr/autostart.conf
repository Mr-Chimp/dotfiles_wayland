# Start your bars script
exec-once = ~/.dotfiles/scripts/hyprland/bars.sh

# Start session target for systemd user services
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --no-block --user start hyprland-session.target